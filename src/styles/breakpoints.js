const size = {
  s1: "420px",
  s2: "850px",
  s3: "925px",
  s4: "1060px",
  s5: "1250px",
};

const breakpoints = {
  size1: `(max-width: ${size.s1})`,
  size2: `(max-width: ${size.s2})`,
  size3: `(max-width: ${size.s3})`,
  size4: `(max-width: ${size.s4})`,
  size5: `(max-width: ${size.s5})`,
};

export default breakpoints;
